#include <stdio.h>
#define MAX 10
int dp[MAX];
int fac(int n){
    if (n==0 || n==1) return 1;
    dp[n]=n*fac(n-1);
    return dp[n];
}
int main()
{
    for(int i=0; i<MAX; i++){
        dp[i]=-1;
    }
    printf("The value of 5 factorial is %d\n",fac(5));

    return 0;
}
